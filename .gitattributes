# Auto detect text files and perform LF normalization
* text=auto

# Explicitly mark binary files as binary to prevent corruption
# Also exclude from LFS processing to prevent filter corruption  
*.png -text -filter
*.jpg -text -filter
*.jpeg -text -filter
*.gif -text -filter
*.ico -text -filter
*.pdf -text -filter