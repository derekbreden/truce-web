# Explicitly mark binary files first to prevent auto-detection issues
*.png -text -diff -filter
*.jpg -text -diff -filter  
*.jpeg -text -diff -filter
*.gif -text -diff -filter
*.ico -text -diff -filter
*.pdf -text -diff -filter

# Auto detect text files and perform LF normalization for remaining files
* text=auto